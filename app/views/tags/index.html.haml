= render :partial => "helpers/title", :locals => { :title => "cloud", :link => root_url }

%p#info_line
  This is a summary of all the tags used on this blog.

- if @tags.size > 0
  %ul#cloud
    - tag_cloud @tags, %w( cloud1 cloud2 cloud3 cloud4 cloud5 cloud6 cloud7 cloud8 cloud9 cloud10 ) do |tag, css_class|
      %li
        = link_to tag.name.downcase, tag_url(tag.name.downcase), :class => css_class
- else
  #error
    %p
      No tags were used in the blog posts

= render :partial => "helpers/top_bottom"